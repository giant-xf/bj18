同步:就是协同步调，按预定的先后次序进⾏运⾏。
异步:就是没有事先预定号好，结果未知。


阻塞:程序需要等待再执行
非阻塞:程序不需要等待


解死锁:
1.添加超时时间
其中，锁定⽅法acquire可以有⼀个blocking参数。
如果设定blocking为True，则当前线程会堵塞，直到获取到这个锁为⽌
（如果没有指定，那么默认为True）
如果设定blocking为False，则当前线程不会堵塞
2.银行家算法


